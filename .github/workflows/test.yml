name: my-test-name
run-name: ${{ github.actor }} is learning GitHub Actions
on: [push]
jobs:
  echo-something:
    runs-on: [self-hosted]
    steps:
      - run: echo This is a test!
      - run: echo Whoop whoop!
