{
  "name": "scottvdev-functions",
  "description": "Firebase Cloud Functions for scottv.dev RAG chatbot",
  "scripts": {
    "build": "tsc && cp ../../src/assets/knowledge/embeddings.json ./embeddings.json",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.3.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "private": true
}
